import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tajlang.netlify',
  appName: 'Tajlang',
  webDir: 'https://tajlang.netlify.app'
};

export default config;
